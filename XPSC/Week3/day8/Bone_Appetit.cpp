#include <bits/stdc++.h>
#include <iomanip>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int m,n,x,y;
    cin>>n>>m>>x>>y;
    cout<<(n*x)+(m*y)<<endl;
    return 0;
}